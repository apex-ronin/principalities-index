# Index artifacts stay on G:\AI-Models\indexes — never in git
*.faiss
*_meta.jsonl
index_manifest.json
.checkpoints/
*.npy

# Embeddings (orphaned Vertex set lives in GCP_RECOVERY; local sets are build artifacts)
*embeddings*

# Python
__pycache__/
*.pyc
.venv/

# Env
.env
